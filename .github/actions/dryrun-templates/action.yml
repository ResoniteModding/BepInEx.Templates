runs:
  using: "composite"
  steps:
    - name: Install Templates
      run: |
        dotnet new --install ./BepInEx.Templates/templates/
      shell: bash
    
    - name: Test BepInEx6 .NET Core Template
      if: always()
      run: |
        dotnet new bep6plugin_resonite --dry-run --diagnostics
      shell: bash